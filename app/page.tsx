'use client';import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import SprinklerLogo from '@/components/SprinklerLogo';
export default function Login(){const r=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');useEffect(()=>{fetch('/api/auth/me').then(x=>x.json()).then(x=>x.user&&r.push('/dashboard'))},[r]);async function submit(e:React.FormEvent){e.preventDefault();setErr('');const x=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});if(x.ok)r.push('/dashboard');else setErr((await x.json()).error)}return <main className="login"><form className="login-card" onSubmit={submit}><SprinklerLogo/><input placeholder="Adresse e-mail" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><input placeholder="Mot de passe" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/><button>Se connecter</button>{err&&<p className="error">{err}</p>}</form></main>}
