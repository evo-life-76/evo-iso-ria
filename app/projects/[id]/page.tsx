'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import {useParams} from 'next/navigation';export default function Project(){const {id}=useParams<{id:string}>();const [items,setItems]=useState<any[]>([]);useEffect(()=>{fetch('/api/isometries?projectId='+id).then(x=>x.json()).then(setItems)},[id]);return <main className="wrap"><Link href="/dashboard">← Accueil</Link><h1>Isométries du chantier</h1><div className="cards">{items.map(x=><Link className="card" href={'/editor/'+x.id} key={x.id}><b>{x.name}</b><span>Modifié le {new Date(x.updatedAt).toLocaleDateString('fr-FR')}</span></Link>)}</div></main>}
