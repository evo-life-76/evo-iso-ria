'use client';import {useEffect,useState} from 'react';import Link from 'next/link';export default function Admin(){const [u,setU]=useState<any[]>([]);const [f,setF]=useState({name:'',email:'',password:''});const load=()=>fetch('/api/users').then(x=>x.json()).then(setU);useEffect(load,[]);async function add(e:React.FormEvent){e.preventDefault();await fetch('/api/users',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});setF({name:'',email:'',password:''});load()}return <main className="wrap"><Link href="/dashboard">← Accueil</Link><h1>Administration</h1><form className="panel" onSubmit={add}><h2>Créer un utilisateur</h2><input placeholder="Nom" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><input placeholder="E-mail" type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input placeholder="Mot de passe" type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><button>Créer le compte</button></form><div className="panel"><h2>Utilisateurs</h2>{u.map(x=><div className="user-row" key={x.id}><b>{x.name}</b><span>{x.email}</span><small>{x.role}</small></div>)}</div></main>}
